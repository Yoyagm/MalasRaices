name: malasraices_app
description: MalasRaices - App de gestión de propiedades en arriendo
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7

  # Networking
  dio: ^5.7.0

  # Routing
  go_router: ^14.8.0

  # Storage
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.3.5

  # UI
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  google_maps_flutter: ^2.10.0
  image_picker: ^1.1.2

  # Utils
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  geolocator: ^13.0.2
  intl: ^0.19.0

  # DI
  get_it: ^8.0.3
  injectable: ^2.5.0
  stream_transform: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.14
  injectable_generator: ^2.6.3
  bloc_test: ^10.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
